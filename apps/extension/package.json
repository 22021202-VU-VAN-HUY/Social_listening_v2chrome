{
  "name": "@listening-social/extension",
  "version": "0.2.5",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build && vite build --config vite.worker.config.ts && vite build --config vite.content.config.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "check": "npm run typecheck && npm run test && npm run build"
  },
  "devDependencies": {
    "@types/chrome": "0.1.4",
    "@types/jsdom": "21.1.7",
    "@types/node": "22.18.8",
    "jsdom": "26.1.0",
    "typescript": "5.9.3",
    "vite": "7.3.6",
    "vitest": "3.2.7"
  }
}
